{
  "name": "location-service-area-toolkit",
  "version": "1.1.0",
  "private": true,
  "description": "Reusable distance, radius/service-area, and Nominatim geocoding utilities.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build:core && npm run build:demo",
    "build:core": "tsc -p tsconfig.json",
    "build:demo": "esbuild demo/app.ts --bundle --format=iife --outfile=demo/app.js",
    "test": "tsx --test tests/*.test.ts",
    "deploy": "npm run build && npx wrangler@latest deploy",
    "deploy:dry": "npm run build && npx wrangler@latest deploy --dry-run"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.20.6",
    "typescript": "^5.7.2",
    "esbuild": "^0.28.2"
  }
}
